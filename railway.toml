# Railway.app configuration
# This tells Railway which build command to use

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python gateway_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
